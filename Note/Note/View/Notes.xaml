<UserControl x:Class="Note.View.Notes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Note"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:Menu ="clr-namespace:Note.Utilities"
             xmlns:view="clr-namespace:Note.View"
             xmlns:vm="clr-namespace:Note.ViewModel" d:DataContext="{d:DesignInstance Type=vm:NavigationVM}"
             mc:Ignorable="d" 
             d:DesignHeight="704" d:DesignWidth="1025">
    <UserControl.DataContext>
        <vm:NoteVM/>
    </UserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"></ColumnDefinition>
            <ColumnDefinition Width="3"></ColumnDefinition>
            <ColumnDefinition ></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Background="#fef8e2">
            <Grid.RowDefinitions>
                <RowDefinition Height="150"></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <TextBlock Style="{StaticResource Text_Style}"
                               Text="Personal"
                               Foreground="Black" HorizontalAlignment="Left" Margin="20,0,0,0"></TextBlock>
                </Grid>
                <Grid Grid.Row="1">
                    <Border BorderBrush="Black" BorderThickness="0,0,0,0.5" >
                        <TextBlock Style="{StaticResource Text_Style}"
                               Text="2 Notes"
                               Foreground="Black"
                               HorizontalAlignment="Left"
                               Margin="20,0,0,0"></TextBlock>
                    </Border>
                </Grid>
            </Grid>
            <Grid Grid.Row="1">
                <ScrollViewer 
                    Style="{StaticResource MaterialDesignScrollViewer}"
                    VerticalScrollBarVisibility="Hidden">
                    <WrapPanel 
                        ItemWidth="300"
                        Height="550" >
                        <!--<view:miniNote Padding="10,10" ></view:miniNote >-->
                        <ListBox ItemsSource="{Binding ListNote}" SelectedItem="{Binding CurNote}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <materialDesign:Card Height="100" Width="280" UniformCornerRadius="10">
                                            <RadioButton
                                                Command="{Binding PageCommand}"
                                                
                                                Style="{DynamicResource MaterialDesignChoiceChipAccentRadioButton}" 
                                                Background="#fff" 
                                                Height="100" 
                                                Width="280" 
                                                Foreground="Black" 
                                                GroupName="GroupNotes">
                                                <Grid VerticalAlignment="Center" 
                                                  HorizontalAlignment="Center" 
                                                  Height="100" 
                                                  Width="280">
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition/>
                                                        <RowDefinition/>
                                                    </Grid.RowDefinitions>
                                                    <TextBlock Grid.Row="0"
                                                        HorizontalAlignment="Left"
                                                        VerticalAlignment="Center"
                                                        Margin="20,0,0,0"
                                                        Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                                        Text="{Binding Title}"
                                                        FontSize="18"/>
                                                    <TextBlock Grid.Row="1"
                                                       HorizontalAlignment="Left"
                                                       VerticalAlignment="Center"
                                                       Margin="25,0,0,0"
                                                       FontSize="12"
                                                       Style="{StaticResource MaterialDesignOverlineTextBlock}"
                                                       Text="{Binding LastEdited}" />
                                                    <TextBlock Grid.Row="1"
                                                        Width="130"
                                                        HorizontalAlignment="Left"
                                                        VerticalAlignment="Center"
                                                        Margin="125,0,0,0"
                                                        Text="{Binding HeadLine}"
                                                        TextWrapping="NoWrap"
                                                        TextTrimming="CharacterEllipsis"
                                                        Style="{StaticResource MaterialDesignOverlineTextBlock}"
                                                        FontSize="14"/>
                                                </Grid>
                                            </RadioButton>
                                        </materialDesign:Card>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </WrapPanel>
                </ScrollViewer>
            </Grid>
        </Grid>
        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch"></GridSplitter>
        <Grid Grid.Column="2" Background="#ffffff">
            <!--<ContentControl>
                <view:Page/>
            </ContentControl>-->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="353*"/>
                <ColumnDefinition Width="8*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="150"></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Grid.ColumnSpan="2">
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Bottom"
                       Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                       Text="{Binding CurNote.Title}"
                       FontSize="32"
                       ></TextBlock>
                </Grid>
                <Grid Grid.Row="1">
                    <ToolBarTray>
                        <ToolBar
                            ClipToBounds="False"
                            Style="{StaticResource MaterialDesignToolBar}" 
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top" 
                            Width="722" 
                            Height="58" 
                            Margin="0,10,0,0">
                            <Button
                                Content="{materialDesign:PackIcon Kind=ContentSave}"
                                ToolTip="Save" 
                                Click="SaveButton" />
                            <Separator />
                            <Button
                                Command="Cut"
                                Content="{materialDesign:PackIcon Kind=ContentCut}"
                                ToolBar.OverflowMode="AsNeeded"
                                ToolTip="Cut"/>
                            <Button
                                Command="Copy"
                                Content="{materialDesign:PackIcon Kind=ContentCopy}"
                                ToolBar.OverflowMode="AsNeeded"
                                ToolTip="Copy that stuff" />
                            <Separator />
                            <Button
                                Command="Paste"
                                Content="{materialDesign:PackIcon Kind=ContentPaste}"
                                ToolBar.OverflowMode="AsNeeded"
                                ToolTip="Paste some stuff" />
                            <!--
      when badging in a toolbar, make sure the parent ToolBar.ClipToBounds="False", and
      manually apply the button style
    -->
                            <Separator />
                            <ListBox
                                SelectedIndex="0">
                                <ListBox.ToolTip>
                                    <StackPanel>
                                        <TextBlock
                                            Text="MaterialDesignToolToggleFlatListBox" />
                                        <TextBlock
                                            Text="Exclusive selection" />
                                        <TextBlock
                                            Text="ListBoxAssist.IsToggle allows more natural toggle behaviour" />
                                    </StackPanel>
                                </ListBox.ToolTip>
                                <ListBoxItem
                                    Content="{materialDesign:PackIcon Kind=FormatAlignLeft}" />
                                <ListBoxItem
                                    Content="{materialDesign:PackIcon Kind=FormatAlignCenter}" />
                                <ListBoxItem
                                    Content="{materialDesign:PackIcon Kind=FormatAlignRight}" />
                                <ListBoxItem
                                    Content="{materialDesign:PackIcon Kind=FormatAlignJustify}" />
                            </ListBox>
                            <Separator/>
                            <ListBox
                                SelectionMode="Extended">
                                <ListBox.ToolTip>
                                    <StackPanel>
                                        <TextBlock
                                            Text="MaterialDesignToolToggleListBox" />
                                        <TextBlock
                                            Text="Multiple selection" />
                                        <TextBlock
                                            Text="ListBoxAssist.IsToggle allows more natural toggle behaviour" />
                                    </StackPanel>
                                </ListBox.ToolTip>
                                <ListBoxItem
                                    Content="{materialDesign:PackIcon Kind=FormatBold}" 
                                    PreviewMouseLeftButtonDown="BoldButton" />
                                <ListBoxItem
                                    Content="{materialDesign:PackIcon Kind=FormatItalic}" 
                                    PreviewMouseLeftButtonDown="ItalicButton" />
                                <ListBoxItem
                                    Content="{materialDesign:PackIcon Kind=FormatUnderline}" 
                                    PreviewMouseLeftButtonDown="UnderLineButton" />
                            </ListBox>
                            <Separator />
                            <Label
                              VerticalAlignment="Center"
                              Content="Font size:" />
                            <ComboBox>
                                <ComboBoxItem
                                    Content="10" />
                                <ComboBoxItem
                                    Content="12"
                                    IsSelected="True" />
                                <ComboBoxItem
                                    Content="14" />
                                <ComboBoxItem
                                    Content="16" />
                            </ComboBox>
                            <Separator />
                            <Separator />
                            <ToggleButton />
                        </ToolBar>
                    </ToolBarTray>
                </Grid>
            </Grid>
            <RichTextBox x:Name="TextBox" Grid.Row="1" Grid.ColumnSpan="2">
                <FlowDocument>
                    <Paragraph>
                        <Run Text="RichTextBox"/>
                    </Paragraph>
                </FlowDocument>
            </RichTextBox>
        </Grid>
    </Grid>
</UserControl>
 